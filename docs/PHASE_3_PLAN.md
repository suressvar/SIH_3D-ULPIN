Phase 3 will add a Next.js 15 web application under frontend and bounded workspace read endpoints to the existing API. Cesium uses the existing GLB geometry in the exported ENU frame; MapLibre provides the 2D fallback. UI selection/view transforms live in Zustand; database results live in TanStack Query. No external imagery, terrain or ownership data will be invented. Missing dashboard image: textual design requirements are the visual basis. Phase 4 work begins only after phase 3 verification. Authentication remains verified JWT plus database membership. Browser tests will use an isolated signed test issuer, clearly separate from real Supabase sign-in.
